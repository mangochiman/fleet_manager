<style>
  @import url('https://fonts.googleapis.com/css2?family=Syne:wght@400;500;600;700;800&family=Inconsolata:wght@300;400;500&display=swap');

  :root {
    --ink:     #0f1117;
    --ink-2:   #2a2d38;
    --ink-3:   #5c6070;
    --paper:   #f7f6f2;
    --surface: #ffffff;
    --border:  #e4e2da;
    --green:   #1a9e6e;
    --green-l: #e8f7f2;
    --red:     #d63d2f;
    --red-l:   #fdf0ee;
    --amber:   #c07b1a;
    --amber-l: #fef6e7;
    --blue:    #2563eb;
    --blue-l:  #eff4ff;
  }

  .ms-wrap {
    font-family: 'Inconsolata', monospace;
    background: var(--paper);
    min-height: 100vh;
    padding: 2rem 2rem 3rem;
    color: var(--ink);
  }

  /* ── Header ── */
  .ms-header {
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    margin-bottom: 2rem;
    padding-bottom: 1.25rem;
    border-bottom: 1.5px solid var(--border);
    gap: 1rem;
    flex-wrap: wrap;
  }

  .ms-eyebrow {
    font-size: 0.65rem;
    font-weight: 500;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--ink-3);
    margin: 0 0 0.3rem;
  }

  .ms-title {
    font-family: 'Syne', sans-serif;
    font-size: 2rem;
    font-weight: 800;
    color: var(--ink);
    margin: 0;
    line-height: 1;
    letter-spacing: -0.03em;
  }

  .ms-title span { color: var(--green); }

  /* ── Buttons ── */
  .ms-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    padding: 0.55rem 0.9rem;
    font-family: 'Inconsolata', monospace;
    font-size: 0.7rem;
    font-weight: 500;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    border-radius: 2px;
    border: 1px solid transparent;
    cursor: pointer;
    text-decoration: none;
    transition: all 0.15s;
    white-space: nowrap;
  }

  .ms-btn-primary {
    background: var(--ink);
    color: #fff;
    border-color: var(--ink);
  }

  .ms-btn-primary:hover {
    background: var(--ink-2);
    box-shadow: 2px 2px 0 var(--green);
    transform: translate(-1px, -1px);
    color: #fff;
  }

  .ms-btn-ghost {
    background: transparent;
    color: var(--ink-3);
    border-color: var(--border);
  }

  .ms-btn-ghost:hover {
    border-color: var(--ink-3);
    color: var(--ink);
    background: var(--surface);
  }

  /* ── Stat Cards ── */
  .ms-stats {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
    gap: 0.85rem;
    margin-bottom: 1.5rem;
  }

  .ms-stat {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 3px;
    padding: 0.9rem 1.1rem;
    position: relative;
    overflow: hidden;
    transition: transform 0.2s, box-shadow 0.2s;
    animation: statIn 0.4s cubic-bezier(0.22,1,0.36,1) both;
  }

  .ms-stat:hover { transform: translateY(-2px); box-shadow: 0 6px 20px rgba(0,0,0,0.07); }

  .ms-stat::before {
    content: '';
    position: absolute;
    left: 0; top: 0; bottom: 0;
    width: 3px;
  }

  .ms-stat.green::before { background: var(--green); }
  .ms-stat.amber::before { background: var(--amber); }
  .ms-stat.ink::before   { background: var(--ink-2); }

  .ms-stat-label {
    font-size: 0.6rem;
    font-weight: 500;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--ink-3);
    margin-bottom: 0.3rem;
  }

  .ms-stat-value {
    font-family: 'Syne', sans-serif;
    font-size: 1.25rem;
    font-weight: 700;
    letter-spacing: -0.02em;
    line-height: 1;
  }

  .ms-stat.green .ms-stat-value { color: var(--green); }
  .ms-stat.amber .ms-stat-value { color: var(--amber); }
  .ms-stat.ink   .ms-stat-value { color: var(--ink); }

  .ms-stat-sub {
    font-size: 0.62rem;
    color: var(--ink-3);
    margin-top: 0.25rem;
  }

  .ms-stat:nth-child(1) { animation-delay: 0.04s; }
  .ms-stat:nth-child(2) { animation-delay: 0.08s; }
  .ms-stat:nth-child(3) { animation-delay: 0.12s; }

  @keyframes statIn {
    from { opacity: 0; transform: translateY(10px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  /* ── Filter bar ── */
  .ms-filters {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 3px;
    padding: 0.85rem 1.1rem;
    margin-bottom: 1rem;
    display: flex;
    gap: 0.6rem;
    flex-wrap: wrap;
    align-items: center;
  }

  .ms-input {
    font-family: 'Inconsolata', monospace;
    font-size: 0.78rem;
    color: var(--ink);
    background: var(--paper);
    border: 1px solid var(--border);
    border-radius: 2px;
    padding: 0.5rem 0.75rem;
    outline: none;
    transition: border-color 0.2s, box-shadow 0.2s;
    letter-spacing: 0.02em;
  }

  .ms-input:focus {
    border-color: var(--ink-2);
    background: #fff;
    box-shadow: 0 0 0 3px rgba(15,17,23,0.07);
  }

  .ms-input-search { flex: 1; min-width: 180px; }
  .ms-input-select { min-width: 120px; }

  .ms-filter-label {
    font-size: 0.62rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--ink-3);
    white-space: nowrap;
  }

  .ms-filter-sep { flex: 1; }

  /* ── Panel ── */
  .ms-panel {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 3px;
    overflow: hidden;
    margin-bottom: 1rem;
  }

  .ms-panel-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0.85rem 1.1rem;
    border-bottom: 1px solid var(--border);
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .ms-panel-title {
    font-family: 'Syne', sans-serif;
    font-size: 0.8rem;
    font-weight: 700;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--ink-2);
    margin: 0;
  }

  /* ── Table ── */
  .ms-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 0.78rem;
  }

  .ms-table thead tr {
    background: var(--paper);
    border-bottom: 1.5px solid var(--border);
  }

  .ms-table th {
    text-align: left;
    padding: 0.6rem 1.1rem;
    font-size: 0.6rem;
    font-weight: 600;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--ink-3);
    white-space: nowrap;
  }

  .ms-table td {
    padding: 0.7rem 1.1rem;
    border-bottom: 1px solid var(--border);
    vertical-align: middle;
  }

  .ms-table tbody tr:last-child td { border-bottom: none; }
  .ms-table tbody tr { transition: background 0.12s; }
  .ms-table tbody tr:hover { background: var(--paper); }

  .ms-customer-name  { display: block; font-weight: 600; color: var(--ink); }
  .ms-customer-phone { display: block; font-size: 0.68rem; color: var(--ink-3); margin-top: 1px; }
  .ms-product-name   { display: block; color: var(--ink); }
  .ms-product-qty    { display: block; font-size: 0.68rem; color: var(--ink-3); margin-top: 1px; }

  .text-green { color: var(--green); font-weight: 500; }
  .text-red   { color: var(--red);   font-weight: 500; }

  /* ── Progress ── */
  .ms-progress {
    height: 3px;
    background: var(--border);
    border-radius: 99px;
    overflow: hidden;
    margin-top: 4px;
    min-width: 60px;
  }

  .ms-progress-fill {
    height: 100%;
    background: var(--amber);
    border-radius: 99px;
  }

  .ms-pct {
    font-size: 0.6rem;
    color: var(--ink-3);
    margin-top: 2px;
    display: block;
  }

  /* ── Badges ── */
  .ms-badge {
    display: inline-block;
    padding: 0.18rem 0.5rem;
    border-radius: 2px;
    font-size: 0.6rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .ms-badge.paid        { background: var(--green-l); color: var(--green); }
  .ms-badge.outstanding { background: var(--amber-l); color: var(--amber); }
  .ms-badge.partial     { background: var(--blue-l);  color: var(--blue); }
  .ms-badge.banked      { background: #f0fdf4;         color: #15803d; }
  .ms-badge.overdue     { background: var(--red-l);   color: var(--red); }

  /* ── Action buttons ── */
  .ms-act {
    display: inline-flex;
    align-items: center;
    padding: 0.3rem 0.6rem;
    font-family: 'Inconsolata', monospace;
    font-size: 0.62rem;
    font-weight: 500;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    border-radius: 2px;
    border: 1px solid var(--border);
    text-decoration: none;
    color: var(--ink-3);
    transition: all 0.12s;
    white-space: nowrap;
  }

  .ms-act:hover { border-color: var(--ink-2); color: var(--ink); background: var(--paper); }

  /* ── Empty state ── */
  .ms-empty {
    text-align: center;
    padding: 3rem 1rem;
    color: var(--ink-3);
  }

  .ms-empty-icon { font-size: 2.5rem; opacity: 0.3; margin-bottom: 0.75rem; }
  .ms-empty p    { font-size: 0.8rem; margin: 0 0 0.5rem; }

  /* ── Pagination ── */
  .ms-pagination {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 0.75rem;
    padding: 0.85rem 1.1rem;
    border-top: 1px solid var(--border);
    font-size: 0.7rem;
    color: var(--ink-3);
  }

  .ms-pagination .pagination {
    display: flex;
    gap: 0.25rem;
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .ms-pagination .pagination li a,
  .ms-pagination .pagination li span {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: 28px;
    height: 28px;
    padding: 0 0.4rem;
    font-family: 'Inconsolata', monospace;
    font-size: 0.7rem;
    border: 1px solid var(--border);
    border-radius: 2px;
    text-decoration: none;
    color: var(--ink-3);
    transition: all 0.12s;
  }

  .ms-pagination .pagination li a:hover { border-color: var(--ink-2); color: var(--ink); background: var(--paper); }
  .ms-pagination .pagination li.active span { background: var(--ink); color: #fff; border-color: var(--ink); }
  .ms-pagination .pagination li.disabled span { opacity: 0.4; }

  /* ── Mobile cards ── */
  .ms-mobile { display: none; }

  .ms-card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 3px;
    margin-bottom: 0.75rem;
    overflow: hidden;
    animation: statIn 0.35s ease both;
  }

  .ms-card-head {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0.65rem 1rem;
    background: var(--paper);
    border-bottom: 1px solid var(--border);
  }

  .ms-card-tid {
    font-family: 'Syne', sans-serif;
    font-size: 0.72rem;
    font-weight: 700;
    color: var(--ink-2);
    letter-spacing: 0.04em;
  }

  .ms-card-body { padding: 0.85rem 1rem; }

  .ms-card-grid {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: 0.35rem 0.75rem;
    margin-bottom: 0.75rem;
    font-size: 0.75rem;
  }

  .ms-card-key {
    font-size: 0.6rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--ink-3);
    padding-top: 1px;
  }

  .ms-card-actions { display: flex; gap: 0.5rem; margin-top: 0.75rem; }
  .ms-card-actions .ms-act { flex: 1; justify-content: center; }

  @media (max-width: 768px) {
    .ms-wrap    { padding: 1rem; }
    .ms-title   { font-size: 1.5rem; }
    .ms-desktop { display: none; }
    .ms-mobile  { display: block; }
    .ms-header-actions .ms-btn span { display: none; }
  }
</style>

<div class="ms-wrap">

  <!-- Header -->
  <div class="ms-header">
    <div>
      <p class="ms-eyebrow">Fleet Management</p>
      <h1 class="ms-title">My <span>Sales</span></h1>
    </div>
    <%= link_to new_sale_path, class: "ms-btn ms-btn-primary" do %>
      <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M12 5v14M5 12h14"/></svg>
      New Sale
    <% end %>
  </div>

  <!-- Stats -->
  <div class="ms-stats">
    <div class="ms-stat green">
      <div class="ms-stat-label">My Total Revenue</div>
      <div class="ms-stat-value"><%= number_to_currency(@total_sales) %></div>
    </div>
    <div class="ms-stat amber">
      <div class="ms-stat-label">My Outstanding</div>
      <div class="ms-stat-value"><%= number_to_currency(@outstanding_sales) %></div>
      <div class="ms-stat-sub"><%= @sales_outstanding_count %> pending payments</div>
    </div>
    <div class="ms-stat ink">
      <div class="ms-stat-label">My Transactions</div>
      <div class="ms-stat-value"><%= @sales_total_count %></div>
    </div>
  </div>

  <!-- Filter bar -->
  <%= form_tag sales_path, method: :get, class: "ms-filters" do %>
    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="color: var(--ink-3); flex-shrink:0;"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.35-4.35"/></svg>
    <%= text_field_tag :search, params[:search], placeholder: "Search by customer…", class: "ms-input ms-input-search" %>
    <%= select_tag :status, options_for_select([["All Status", ""], ["Outstanding", "outstanding"], ["Partial", "partial"], ["Paid", "paid"], ["Banked", "banked"]], params[:status]), class: "ms-input ms-input-select" %>
    <span class="ms-filter-label">Show</span>
    <%= select_tag :per_page, options_for_select([10, 20, 50, 100], params[:per_page] || 20), class: "ms-input", style: "width: 70px;", onchange: "this.form.submit()" %>
    <span class="ms-filter-sep"></span>
    <%= submit_tag "Filter", class: "ms-btn ms-btn-primary" %>
    <%= link_to "Clear", sales_path, class: "ms-btn ms-btn-ghost" %>
  <% end %>

  <!-- ── DESKTOP TABLE ── -->
  <div class="ms-panel ms-desktop">
    <div class="ms-panel-head">
      <h3 class="ms-panel-title">All My Sales</h3>
    </div>

    <% if @sales.empty? %>
      <div class="ms-empty">
        <div class="ms-empty-icon">🧾</div>
        <p>No sales found.</p>
        <%= link_to "Create your first sale →", new_sale_path, class: "ms-btn ms-btn-primary" %>
      </div>
    <% else %>
      <table class="ms-table">
        <thead>
          <tr>
            <th>Customer</th>
            <th>Product</th>
            <th>Amount</th>
            <th>Paid</th>
            <th>Balance</th>
            <th>Status</th>
            <th>Date</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <% @sales.each do |sale| %>
            <tr>
              <td>
                <span class="ms-customer-name"><%= sale.customer_name %></span>
                <span class="ms-customer-phone"><%= sale.customer_phone %></span>
              </td>
              <td>
                <span class="ms-product-name"><%= sale.product.name %></span>
                <span class="ms-product-qty"><%= sale.quantity %> <%= sale.product.unit %></span>
              </td>
              <td class="text-green"><%= number_to_currency(sale.total_amount) %></td>
              <td class="text-green"><%= number_to_currency(sale.paid_amount || 0) %></td>
              <td class="<%= sale.remaining_balance > 0 ? 'text-red' : 'text-green' %>">
                <%= number_to_currency(sale.remaining_balance) %>
              </td>
              <td>
                <span class="ms-badge <%= sale.payment_status %>"><%= sale.payment_status.titleize %></span>
                <% if sale.partial? %>
                  <div class="ms-progress">
                    <div class="ms-progress-fill" style="width: <%= sale.payment_percentage %>%;"></div>
                  </div>
                  <span class="ms-pct"><%= sale.payment_percentage %>% paid</span>
                <% end %>
              </td>
              <td><%= sale.transaction_date.strftime("%-d %b %Y") %></td>
              <td>
                <%= link_to "View", sale, class: "ms-act" %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    <% end %>

    <% if @sales_total_count > 0 && @sales.respond_to?(:total_pages) %>
      <div class="ms-pagination">
        <span>
          <% if @sales.total_pages > 1 %>
            <%= @sales.offset + 1 %>–<%= [@sales.offset + @sales.length, @sales_total_count].min %> of <%= @sales_total_count %> transactions
          <% else %>
            All <%= @sales_total_count %> transactions
          <% end %>
        </span>
        <% if @sales.total_pages > 1 %>
          <%= will_paginate @sales,
                renderer: WillPaginate::ActionView::BootstrapLinkRenderer,
                previous_label: '←',
                next_label: '→',
                class: 'pagination' %>
        <% end %>
      </div>
    <% end %>
  </div>

  <!-- ── MOBILE CARDS ── -->
  <div class="ms-mobile">
    <% @sales.each_with_index do |sale, i| %>
      <div class="ms-card" style="animation-delay: <%= i * 0.04 %>s;">
        <div class="ms-card-head">
          <span class="ms-card-tid"><%= sale.transaction_id %></span>
          <span class="ms-badge <%= sale.payment_status %>"><%= sale.payment_status.titleize %></span>
        </div>
        <div class="ms-card-body">
          <div class="ms-card-grid">
            <span class="ms-card-key">Customer</span>
            <span><%= sale.customer_name %><br><small style="color:var(--ink-3)"><%= sale.customer_phone %></small></span>

            <span class="ms-card-key">Product</span>
            <span><%= sale.product.name %> &mdash; <small><%= sale.quantity %> <%= sale.product.unit %></small></span>

            <span class="ms-card-key">Amount</span>
            <span class="text-green"><%= number_to_currency(sale.total_amount) %></span>

            <span class="ms-card-key">Paid</span>
            <span><%= number_to_currency(sale.paid_amount || 0) %></span>

            <span class="ms-card-key">Balance</span>
            <span class="<%= sale.remaining_balance > 0 ? 'text-red' : 'text-green' %>"><%= number_to_currency(sale.remaining_balance) %></span>

            <span class="ms-card-key">Date</span>
            <span><%= sale.transaction_date.strftime("%-d %b %Y") %></span>
          </div>

          <% if sale.partial? %>
            <div class="ms-progress" style="margin-bottom: 0.35rem;">
              <div class="ms-progress-fill" style="width: <%= sale.payment_percentage %>%;"></div>
            </div>
            <span class="ms-pct" style="text-align:center; display:block;"><%= sale.payment_percentage %>% paid</span>
          <% end %>

          <div class="ms-card-actions">
            <%= link_to "View Details", sale, class: "ms-act" %>
          </div>
        </div>
      </div>
    <% end %>

    <% if @sales.empty? %>
      <div class="ms-empty">
        <div class="ms-empty-icon">🧾</div>
        <p>No sales found.</p>
        <%= link_to "Create your first sale →", new_sale_path, class: "ms-btn ms-btn-primary" %>
      </div>
    <% end %>

    <% if @sales_total_count > 0 && @sales.respond_to?(:total_pages) && @sales.total_pages > 1 %>
      <div style="padding: 0.5rem 0;">
        <%= will_paginate @sales,
              renderer: WillPaginate::ActionView::BootstrapLinkRenderer,
              previous_label: '←',
              next_label: '→',
              class: 'pagination' %>
      </div>
    <% end %>
  </div>

</div>
